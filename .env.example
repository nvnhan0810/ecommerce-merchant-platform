# Root env pointer — API đọc api/.env (copy từ api/.env.example).
# FE prod build dùng VITE_API_BASE_URL trong từng app .env / install.sh.

# Copy:
#   cp api/.env.example api/.env
# rồi điền DB_USERNAME / DB_PASSWORD / DB_DATABASE.
